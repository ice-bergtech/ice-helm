apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: data-peertube
  name: data-peertube
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
